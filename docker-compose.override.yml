# Override file for EC2 production deployment
# This forces the 'edge' tag (latest build from main branch)
# Matches /opt/statuswatch/docker-compose.override.yml

services:
  web:
    image: ghcr.io/kontentwave/statuswatch-web:edge

  worker:
    image: ghcr.io/kontentwave/statuswatch-web:edge

  beat:
    image: ghcr.io/kontentwave/statuswatch-web:edge
